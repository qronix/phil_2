<div id='rolesPanel'>
    {{#each roles}}
        <div class='roleRow'>
            <span class='roleName'>{{this.rolename}} ({{this.count}})</span>
            <span class="roleArrow">></span>
            <div class='editRolePanel'>
                {{#each this.permissions as |value|}}
                    {{#notInitValue @key '$init' value}}
                    {{/notInitValue}}
                {{/each}}
            </div>
        </div>
    {{/each}}
</div>
